<%= simple_form_for [city, post, comment], remote: true do |f| %>
  <%= f.input :content, label: false, as: :text, input_html: {class: 'form-control', rows: "10"} %>
  <%= f.button :submit, 'Submit Comment' %>
<% end %>