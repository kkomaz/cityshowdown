<% if logged_in? %>
<h1 class="center", id="headline"><b>Hello <%= current_user.nickname %>!</b></h1>
<h4 class="center intro">Which city would you like to see?</h4>
<br>
<% else %>
<h1 class="center", id="headline">Which city would you like to see?</h1><br>
<% end %>

<div class="container center">
	<div id="wrapper">
		<div id="content">
			<div id="slider" class="span">
				<% @cities.each.with_index do |city, index| %>
					<div class="img image-<%= index+1 %>">
					<%= link_to city.name, city, {:class => 'btn btn-success city_name', :style => "width: 50%"} %>
					<%= link_to image_tag(city.image, width: "90%"), city %>
					</div>
				<% end %>
				<div class="nav previous"></div>
				<div class="nav next"></div>
			</div>
			<div id="dots">
				<ul></ul>
			</div>
		</div>
	</div>
</div>